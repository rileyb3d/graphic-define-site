# Set in Vercel dashboard. For local dev with `vercel dev`, create .env and add:

# Chat widget (api/chat.ts)
GEMINI_API_KEY=

# Contact form → email to riley@graphicdefine.com (api/contact.ts). Get key at resend.com
RESEND_API_KEY=

# Optional: custom "From" for contact emails (e.g. contact@graphicdefine.com after verifying domain in Resend). Defaults to onboarding@resend.dev.
# CONTACT_FROM_EMAIL=
